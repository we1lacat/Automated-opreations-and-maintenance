# Shell 编程

从运维管理的角度看，shell 是一门出色而强大的工具。

得益于良好的可移植性，一份 shell 脚本在绝大多数 shell 环境里都能直接使用。

直到今天，shell 依然是被大量运维工程师学习的热门技能。

本笔记面向想快速上手 shell 编程的读者，偏实操，理论从简。

> **术语约定**（全文统一）：
> - 命令、代码、路径、文件名保留英文原文
> - **父 shell / 子 shell**（parent shell / subshell）、**内置命令**（built-in command）、**环境变量**（environment variable）首次出现标注英文，后文直接用中文
> - bash 作为程序名统一小写；句首和标题中用 Bash / Shell

## shebang（解释器声明）

Q：什么是 shebang？为什么要学？

A：shebang 必须是文件的**绝对第一行**，用来告诉系统该用哪个解释器执行这个文件。

*前面不允许有空行、空格或任何字符！*

> #!interpreter [可选参数]
>
> - **Bash/Shell**：`#!/bin/bash` 或 `#!/usr/bin/env bash`
> - **Python**：`#!/usr/bin/env python3`
> - **Node.js**：`#!/usr/bin/env node`
> - **Perl**：`#!/usr/bin/perl`
>
> ##### "env 技巧"（最佳实践）
>
> 经常能看到 `#!/usr/bin/env python3` 而不是 `#!/usr/bin/python3`：
>
> - `#!/usr/bin/python3` 假定 Python 一定装在那个路径。用户把 Python 装到别处，脚本就挂了。
> - `#!/usr/bin/env python3` 会去用户的 `$PATH` 环境变量里找 `python3`，脚本在**不同 Linux/macOS 系统间可移植性大大提高**。

## Bash 特性

Q：bash 和 shell 是什么关系？

**"shell" 是一个通用概念，bash 是实现这个概念的一款具体软件。**

打个比方：

Shell = **"汽车"**（带轮子和发动机的交通工具这个大类）

Bash = **"丰田凯美瑞"**（某个具体的品牌和型号）

就现阶段学习而言，bash 完全够用：

- *bash 是一个命令解释器，运行在文本终端里，可以直接执行用户输入的命令。*
- *bash 可以读取写在文件里的 Linux 命令，这种文件就叫脚本（script）。*
- *bash 支持通配符、管道、命令替换、逻辑控制语句。*

```text
        ┌──────────────────────────────┐
        │             用户             │
        └──────────────┬───────────────┘
                       │  输入命令 / 脚本
                       ▼
        ┌──────────────────────────────┐
        │         shell / bash         │  ← 命令解释器
        │  解析命令 · 通配符 · 管道       │
        │  命令替换 · 逻辑控制语句        │
        └──────────────┬───────────────┘
                       │  系统调用
                       ▼
        ┌──────────────────────────────┐
        │          Linux 内核           │
        └──────────────┬───────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │             硬件              │
        └──────────────────────────────┘
```

> shell 就是用户和内核之间的"翻译官"：把人能看懂的命令翻译成系统调用，交给内核执行。

`shell 会保存当前会话中用户执行过的命令`

> history　#查看历史命令及参数
>
> -c　　#清空内存中的命令历史
>
> -r　　#从历史文件中恢复命令历史
>
> -数字　#显示最近 N 条命令
>
> !!　　#执行上一条历史命令

## shell 执行环境（父 shell 与子 shell）

Q：为什么 shell 会存在执行环境的差异？不同执行方式有什么区别？

A：

1. 用 `bash` 新开进程执行脚本时会创建子 shell，**当前 shell 的变量不会保留**（可用 `pstree` 查看进程树验证）。
2. 用 `source` 执行脚本是在**当前 shell 里加载**，变量会保留。

#### 父 shell 与子 shell

*执行方式决定了脚本跑在哪个 shell 里：*

- `source` 和 `.`（点命令）：在**当前 shell 环境**中执行脚本并生效
- 显式指定 `bash` 或 `sh` 解释器运行脚本：会**新开一个子 shell**（subshell），命令在子 shell 里执行
- `./script`：按 shebang 指定的解释器执行，同样跑在子 shell 里

##### 父 shell（查看父 shell）

两种方法：

> pstree
>
> ps -ef --forest　#-f 显示 uid、pid、ppid；-e 列出所有进程信息，同 -A

##### 子 shell（创建进程列表）

顺序执行一串 shell 命令：

> ls;cd;pwd;echo "love"　#分号只是顺序执行

要形成"进程列表"的概念，需要用 `( )` 圆括号，如下称为进程列表（process list）：

> (cd ~;pwd;ls ;cd /tmp/;pwd ;ls;)

判断当前是否在子 shell 里：

> Linux 有个和 shell 相关的默认变量：
>
> $BASH_SUBSHELL　　#值为 0 表示父 shell，否则就是子 shell
>
> (cd ~;pwd;ls ;cd /tmp/;pwd ;ls;echo $BASH_SUBSHELL)　#测试是否处于子 shell

##### shell 结构

> parent shell（父 shell）
>
> ​				->	child shell（子 shell）
>
> ​									->	child shell
>
> ​													->		child shell

输入 `exit` 可以退出子 shell。

用子 shell 完成任务后就退出；中断 shell 也会退出。

##### 子 shell 的嵌套运行

子 shell 可以嵌套。利用圆括号，就能层层开出子 shell 并验证。在 shell 脚本开发里，子 shell 常用于多进程处理，提高程序的并发度和执行效率。

> (pwd;(pwd;(echo $BASH_SUBSHELL)))

```text
( pwd ; ( pwd ; ( echo $BASH_SUBSHELL ) ) )

父 shell                       $BASH_SUBSHELL = 0
│
└─( 第 1 层子 shell            $BASH_SUBSHELL = 1
   │
   └─( 第 2 层子 shell         $BASH_SUBSHELL = 2
      │
      └─( 第 3 层子 shell      $BASH_SUBSHELL = 3  ← echo 输出 3
```

> 一对 `( )` 就往下开一层子 shell。子 shell 全部退出后，控制权回到父 shell；子 shell 里发生的一切（cd、变量赋值）对父 shell 不可见。

`shell 中变量的加载顺序、父子 shell 之间变量的传递，都遵循固定规则。`

谈继承之前，先分清两类变量：

- **本地变量（Local/Shell Variable）**：`myvar="hello"` 这样定义，**当前 shell 私有**。
- **环境变量（Environment/Exported Variable）**：`export myvar="hello"`（或定义后 `export`），会被**复制**到所有子 shell。

环境变量通常指用 `export` 内置命令导出的变量，主要用于定义 shell 的运行环境，保证 shell 命令正确执行。

shell 依靠环境变量判断登录用户名、路径、文件系统等信息。

环境变量可以临时创建，但退出终端就会丢失。

```text
        变量的生效范围与生命周期
 ┌───────────────────────────────────────────────┐
 │  export myvar=1   （写在当前终端）              │
 │    生效范围：当前会话 + 子 shell                │
 │    生命周期：终端一关就没了        ← 临时        │
 ├───────────────────────────────────────────────┤
 │  写进 ~/.bashrc   （用户级）                    │
 │    生效范围：当前用户的每个新 shell             │
 │    生命周期：永久（每次开 shell 重新加载）        │
 ├───────────────────────────────────────────────┤
 │  写进 /etc/profile （系统级，需 root）           │
 │    生效范围：所有用户的所有登录 shell            │
 │    生命周期：永久                              │
 └───────────────────────────────────────────────┘
```

> 临时定义 → 关终端就丢；想永久生效，必须写进配置文件（改完记得 `source` 一下或重开终端）。

```text
                 父 shell
                 myvar="hello"        ← 本地变量，只有自己看得见
                 export envvar=1      ← 环境变量，fork 时复制给子 shell
                      │ fork
        ┌─────────────┴─────────────┐
        ▼                           ▼
   子 shell A                   子 shell B
   echo $myvar   → 空           echo $envvar → 1（拿到的是副本）
   echo $envvar  → 1            envvar=2 改的是自己的副本，
                                父 shell 里的 envvar 仍然是 1
```

> **父子 shell 铁律**：变量只能"向下复制"，不能"向上回传"。
> - 本地变量：子 shell 完全看不到
> - 环境变量：子 shell 拿到的是**副本**，改了不影响父 shell
> - 想把子 shell 的值传回父 shell：只能靠输出（`echo` 回传）或写文件，直接改是不可能的

#### `.` 与 source 命令

`.` 和 `source` 都是在**当前 shell** 里加载脚本及其变量。想了解更多用法见 shell 函数一节。

补充：

- 如果脚本写的是 `#!/bin/bash`，建议用 **source** —— 读代码的人一眼就能看出你在加载外部文件。
- 如果脚本写的是 `#!/bin/sh`（追求最大兼容），就**必须**用 **`.`**，因为 `source` 在 sh 里不保证存在。

## shell 变量

- 定义与赋值：变量名和等号和值之间**不能有空格**

> name="we1l"
>
> 变量名
>
> 变量类型：bash 默认把所有变量当作字符串
>
> bash 变量是弱类型，无需预先声明类型，声明和赋值同时完成

- 变量替换与变量引用

> name="we1l"
>
> echo ${name}
>
> echo $name　　#花括号可以省略

- 变量命名规范
  - 名字要准确、见名知义，禁止使用保留关键字
  - 只能包含数字、字母、下划线
  - 不能以数字开头
  - 不能有标点符号
  - 严格区分大小写

> 有效的写法
>
> Name_me
>
> NameMe
>
> 无效的写法
>
> %me
>
> me-name

- 变量作用域
  - 本地变量：仅当前 shell 进程可用
  - 环境变量（也叫全局变量）：当前 shell 及其子 shell 都可用；又分为自定义环境变量和内置环境变量
  - 局部变量：仅 shell 函数或脚本内可用

- 位置参数变量：用于向 shell 脚本传递参数

- 特殊变量：shell 内置的具有特殊功能的变量

  - $?

    ​		0：执行成功

    ​		1-255：错误码

  ```
  $?  上一条命令的返回状态，0 为成功，非 0 为失败
  $$  当前 shell 脚本的进程 ID（PID）
  $!  最近一个后台进程的 PID
  $_  上一条命令的最后一个参数
  查询方法：man bash 后搜索 Special Parameters
  ```

- 自定义变量

  - 变量赋值：varName=value

  - 变量引用：${varName}

    *引用时引号的使用影响很大*

    ''　：强引用，关闭解析，原样输出

    ""　：弱引用，输出双引号内所有内容，会识别特殊符号

    不加引号：不推荐；连续变量可用，但存在空格时会产生歧义

    反引号 ``：命令替换，引用命令的执行结果，等价于 $()

    > n1=1
    >
    > n2=2
    >
    > n3="$n1"　#替换为变量的值　　　　-""
    >
    > n4='$n1'　#被当作普通字符串　　-''

## shell 字符串操作

#### 字符串基本语法

*学字符串操作之前，先掌握基本语法：*

> ${variable}　返回变量的值
>
> ${#variable}　variable="apple" 时返回字符串长度
>
> ${variable:start}　返回变量值从偏移量 start 之后的部分
>
> ${variable:start:length}　返回从偏移量 start 起、长度受 length 限制的部分
>
> ${variable#word}　从变量值**开头**删除最短匹配 word 的部分
>
> ${variable##word}　从变量值**开头**删除最长匹配 word 的部分
>
> ${variable%word}　从变量值**结尾**删除最短匹配 word 的部分
>
> ${variable%%word}　从变量值**结尾**删除最长匹配 word 的部分
>
> ${variable/pattern/string}　把第一个匹配 pattern 的部分替换为 string
>
> ${variable//pattern/string}　把所有匹配 pattern 的部分都替换为 string

#### 字符串切片

对 shell 变量做切片一般有两种方式：按指定位置切片、按指定字符（子串）切片。

##### 按指定位置切片

这种方式需要两个参数：除了起始位置，还要给切片长度，才能确定最终提取的子串。

既然要指定起始位置，就有从左数还是从右数的问题 —— 答案是：**两种都支持**。

###### 1）从字符串左边计数

从左边计数的格式：

```
${string: start :length}
```

`string` 是待切片的字符串，`start` 是起始位置（从左数，**从 0 开始**），`length` 是切片长度（省略则取到串尾）。

###### 2）从字符串右边计数

从右边计数时，起始偏移量必须用空格隔开，否则 shell 会把它当成算术减法。格式：`${string: -start :length}`

> ⚠️ 注意：**减号 `-` 前面必须有一个空格**。

- `string`：待切片的目标字符串
- `-start`：从字符串右端数起的起始位置，**从 1 开始**（最右边的字符是 1）
- `length`：提取的字符数，省略则一直取到串尾

快速示例

```
str="ABCDEFG"
# 从右边第 3 位开始取 3 个字符
echo ${str: -3:3}
# 输出：EFG
```

对比小结

| 语法 | 方向 | 起始下标 | 示例 |
| ---- | ---- | -------- | ---- |
| `${str:start:length}` | 从左向右 | `0` | `${str:0:2}` |
| `${str: -start:length}` | 从右向左 | `1` | `${str: -3:2}` |

常见坑

`${str:-3}`　错误 —— 这是**默认值替换**，不是切片。

`${str: -3}`　正确 —— 减号前加空格才是右起切片。

#### 字符串长度统计

统计字符串长度有好几种方法，效率各不相同：

```shell
1. `${#variable}`    ：Bash 内置，性能最高（推荐）
2. `expr length "$str"`：POSIX 兼容，外部命令
3. `echo -n "$str" | wc -c`：按字节计数，注意换行符
4. `awk '{print length($0)}'`：适合批量文本处理
```

`编写 shell 时尽量使用内置命令`（不启动外部进程，效率高）

#### 变量处理

- 变量值为空时，返回 word（不改变变量值）

> ${parameter:-word}

- 变量为空时，用 word 给变量赋值，并返回该值

> ${parameter:=word}

- 变量为空时，把 word 输出到 stderr 报错；否则返回变量值
  用于变量为空导致出错时返回错误信息

> ${parameter:?word}

- 变量为空时，什么都不返回；非空时返回 word

> ${parameter:+word}

## shell 命令

#### 内置命令与外部命令

什么是内置命令，什么是外部命令？

**内置命令（built-in command）**：系统启动时加载进内存、常驻内存，执行效率高，但占用资源，例如 `cd`。

**外部命令（external command）**：系统需要从磁盘读取程序文件，再载入内存执行。

外部命令也叫文件系统命令 —— 独立存放于磁盘上的程序，是 bash 之外的东西。

```bash
/bin
/usr/bin
/sbin
/usr/sbin

[root@chaogelinux tmp]# which cd
/usr/bin/cd
```

例如 `ps` 命令。

> 用 Linux 的 `type` 命令可以验证一个命令是内置还是外部命令

###### 外部命令的特点：

一定会启动子 shell 进程来执行

###### 内置命令的特点：

内置命令不会创建子 shell 来执行

内置命令和 shell 集成在一起，是 shell 的一部分，不需要读取其他文件，系统启动后就可用

可以用 `type` 命令验证：

```bash
compgen -b    #列出所有内置命令
```

#### 常用命令

> read 是内置命令，用于读取输入的参数
>
> -p　#设置提示信息
>
> -t 数字　#等待超时时间
>
> 输入多个参数时用空格分隔
>
> 最后用变量接收值

## shell 脚本开发

Q：shell 脚本编程有依赖或库函数吗？

A：Shell **没有** C 语言那种编译型的共享库（`.so` / `.dll`），也没有 Python 那种 import 式的模块系统。但有等价的概念：内置命令、外部依赖、可复用的脚本库。

### shell 运算

*整数运算 shell 有原生方法；浮点运算则要借助其他工具*

#### 常用算术运算命令

| 运算命令 | 说明 |
| -------- | ---- |
| (()) | 整数运算常用方式，效率很高 |
| let | 整数运算，类似 (()) |
| expr | 可用于整数运算，还有不少附加功能 |
| bc | Linux 下的计算器程序（整数、浮点都行） |
| $[] | 整数运算 |
| awk | 整数、浮点运算都可以 |
| declare | 定义变量值和属性；-i 参数可定义整数变量用于运算 |

###### 常用算术运算符

| 运算符 | 说明（* 表示常用） |
| ------ | ------------------ |
| `+`、`-` | 加法（或正号）、减法（或负号）* |
| `*`、`/`、`%` | 乘法、除法、取余（取模）* |
| `**` | 乘方 * |
| `++`、`--` | 自增、自减，可放在变量前或变量后 * |
| `!`、`&&`、`\|\|` | 逻辑非、逻辑与、逻辑或 * |
| `<`、`<=`、`>`、`>=` | 比较运算符（小于、小于等于、大于、大于等于） |
| `==`、`!=`、`=` | 相等、不等；字符串比较中 `=` 等价于 `==` * |
| `<<`、`>>` | 左移、右移 |
| `~`、`\|`、`&`、`^` | 按位非、按位或、按位与、按位异或 |
| `=`、`+=`、`-=`、`*=`、`/=`、`%=` | 赋值运算符，如 `a+=1` 等价于 `a=a+1` * |

##### (())

```bash
# 赋值运算：双小括号里不用加 $，变量直接写
a=10
((a += 5))          # 等价于 a=a+5
echo $a             # 15

# 表达式求值：取结果要在外面套 $(( ))
echo $((a * 2 + 1)) # 31
echo $((10 / 3))    # 3   ← 只做整数除法，小数直接砍掉

# 自增自减
i=0
((i++))             # i=i+1
echo $i             # 1

# 比较逻辑：成立返回 1，不成立返回 0
((3 > 2))           # echo $? → 0（真）
((2 > 3))           # echo $? → 1（假）

# 运算赋值一步到位
((b = a ** 2))      # 乘方
echo $b             # 225
```

> `(())` 只吃整数。要浮点运算，用 `bc` 或 `awk`（见下面小节）。

##### let

等价于 `(())`，但效率更高

##### expr

一个简单的计算器命令

```
expr --help    #查看更多用法
```

示例：

```bash
expr 5 + 3        #expr 命令对格式不友好，参数之间必须有空格
expr 5 \* 3       #特殊字符用 '\' 转义
expr 5 \> 3
expr length apple #用 expr 的参数可获得不同结果
```

###### expr 模式匹配

> expr 支持模式匹配
>
> 两个特殊符号：
>
> `:`　　：统计字符串字符数
>
> `.*`　：任意字符串重复 0 次或任意多次
>
> 语法：expr String ":" ".*"

### shell 测试

> shell 提供了条件测试语法
> test 命令
> [ ] 方括号

#### 条件测试常用语法

| 条件测试语法 | 说明 |
| :----------- | :--- |
| 语法一：`test <测试表达式>` | 用 `test` 命令做条件测试。`test` 命令和 `<测试表达式>` 之间至少有一个空格 |
| 语法二：`[ <测试表达式> ]` | 单中括号 `[]`，用法和 `test` 命令相同；这是老男孩推荐的方式。括号边界和内容之间至少有一个空格 |
| 语法三：`[[ <测试表达式> ]]` | 双中括号 `[[]]`，是比 `test` 和 `[]` 更新的语法。括号边界和内容之间至少有一个空格 |
| 语法四：`(( <测试表达式> ))` | 双小括号 `(())`，一般用在 `if` 语句里。`(())` 两端不需要空格 |

#### test 条件测试

test 命令评估一个表达式，真返回 0，假返回 1（配合 `$?` 取值判断）。

> **注意**：`test` 的返回值约定和 C 语言相反 —— **0 表示真，非 0 表示假**。

##### test 命令参数

> 1. 关于文件"类型"的检测（存在与否），如 `test -e filename`
>
> -e：文件是否存在？（常用）
> -f：是否为普通文件？（常用）
> -d：是否为目录？（常用）
> -b：是否为块设备？
> -c：是否为字符设备？
> -S：是否为 Socket 文件？
> -p：是否为 FIFO（管道）文件？
> -L：是否为符号链接？
>
> 2. 关于文件权限的检测，如 `test -r filename`
>
> -r：检测文件是否可读？
> -w：检测文件是否可写？
> -x：检测文件是否可执行？
> -u：检测文件是否有 SUID 属性？
> -g：检测文件是否有 SGID 属性？
> -k：检测文件是否有 Sticky bit 属性？
> -s：检测文件是否为非空文件？
>
> 3. 两个文件之间的比较，如：`test file1 -nt file2`
>
> -nt（newer than）：判断 file1 是否比 file2 新
> -ot（older than）：判断 file1 是否比 file2 旧
> -ef：判断两个文件是否为同一个文件（指向同一个 inode），常用于硬链接判断
>
> 4. 两个整数之间的判断，如：`test n1 -eq n2`
>
> -eq：两值相等（equal）
> -ne：两值不等（not equal）
> -gt：n1 大于 n2（greater than）
> -lt：n1 小于 n2（less than）
> -ge：n1 大于等于 n2（greater or equal）
> -le：n1 小于等于 n2（less or equal）
>
> 5. 字符串的判断
>
> `test -z string`　判断字符串是否为空？为空则真
> `test -n string`　判断字符串是否非空？为空则假
> 注意：-n 可以省略
> `test str1 = str2`　判断 str1 是否等于 str2，相等则真
> `test str1 != str2`　判断 str1 是否不等于 str2，相等则假
>
> 6. 多重条件判断，如：`test -r filename -a -x filename`
>
> -a（and）：两个条件同时成立才为真！如 `test -r file -a -x file`，文件同时有 r 和 x 权限才返回真
> -o（or）：两个条件任一成立即为真！如 `test -r file -o -x file`，文件有 r 或 x 权限就返回真
> !　：取反。如 `test ! -x file`，文件没有 x 权限时返回真

#### 中括号条件测试 [ ]

脚本里做条件测试最常用的是 `[ ]`。

`test` 和 `[ ]` 用法相同。

特别强调：**括号内侧的空格必不可少**。

> |
>
> 注意
>
> 条件里使用变量时必须加 " "
>
> [ -n "$filename" ]

`值得一提的是 [[ ]]`

`[[ ]]` 是 `[ ]` 的增强版，支持正则处理。

`[[ ]]` 里不需要转义字符。

日常工作中单中括号用得最多，双中括号属于特殊场景下的扩展语法。

另外，双中括号同样支持 `-eq`、`-lt`、`<`、`>`、`=`。

#### 字符串比较测试

| 常用字符串测试操作符 | 说明 |
| :------------------- | :--- |
| `-n "string"` | 字符串长度不为 0 时为真，即测试表达式成立。`n` 可以理解为 `no zero` |
| `-z "string"` | 字符串长度为 0 时为真，即测试表达式成立。`z` 可以理解为 `zero` 的缩写 |
| `"str1" = "str2"` | 字符串 1 等于字符串 2 时为真。可以用 `==` 替代 |
| `"str1" != "str2"` | 字符串 1 不等于字符串 2 时为真。注意：**不能用 `!==` 替代** |

比较两个字符串变量的值，判断相等或不等的场景。

> - `=`　：判断是否相等
> - `!=`　：判断是否不等
> - `!`　　：对结果取反，真假互换

> 注意
> 字符串变量做比较时
> 永远记得给变量加双引号
> 用于检查变量值是否相等

#### 数值比较测试

| `[]` 和 `test` 用的比较符 | `(())` 和 `[[]]` 用的比较符 | 说明 |
| :------------------------ | :-------------------------- | :--- |
| `-eq` | `==` 或 `=` | 相等（equal） |
| `-ne` | `!=` | 不等（not equal） |
| `-gt` | `>` | 大于（greater than） |
| `-ge` | `>=` | 大于等于（greater or equal） |
| `-lt` | `<` | 小于（less than） |
| `-le` | `<=` | 小于等于（less or equal） |

1. 在中括号和 `test` 中做数值比较

在中括号里使用数学比较符号时，必须加转义符

```
# \>
# test 和 [ ] 语法支持 -eq 和 < > = !=
```

#### 逻辑运算符

> &&　-a　AND 运算，两边都为真结果才为真
>
> ||　　-o　OR 运算，两边有一边为真结果就为真

| `[]` 和 `test` 用的运算符 | `[[ ]]` 和 `(())` 用的运算符 | 说明 |
| :----------------------- | :--------------------------- | :--- |
| -a | && | 与：两端都为真，结果才为真 |
| -o | \|\| | 或：一端为真，结果就为真 |
| ! | ! | 非：两端相反时结果为真 |

### shell 流程控制与函数

#### if 语句

```
#单分支 if
if  <条件表达式>
	then
        代码...
fi

#简化写法
if <条件表达式>; then
    代码....
fi

#嵌套 if
if <条件表达式>
then
    代码 1....
      if <条件表达式>
    	then
        代码 2...
    fi
fi

#if-else
if <条件表达式>

    then

        条件成立时执行我....（命令集 1）

else

    否则执行我....（命令集 2）

fi

#多分支 if
#语法
#if：如果... 那么
#elif：否则如果... 那么
#else：否则...（不需要 then）

if <条件表达式>
    then
        代码 1
elif <条件表达式 2>
    then
   		代码 2
elif <条件表达式 3>
		代码 3
fi		
```

#### case 语句

`case` 语句的基本结构：

```bash
case "$variable" in
    pattern1)
        # 匹配到 pattern1 时执行的命令
        ;;
    pattern2)
        # 匹配到 pattern2 时执行的命令
        ;;
    *)
        # 默认分支（前面的模式都不匹配时执行）
        ;;
esac
```

**关键语法元素：**

- **`case`**：语句的开始关键字。
- **`$variable`**：被求值的表达式或变量。**强烈建议加双引号**（如 `"$variable"`），防止变量为空或带空格时报错。
- **`in`**：分隔变量和模式列表的关键字。
- **`pattern)`**：匹配条件，右括号 `)` 标记模式的结束。
- **`;;`**：每个分支的结束符，类似其他语言里的 `break` —— 执行完匹配分支后跳出整个 `case` 结构。
- **`\*)`**：默认通配模式，匹配前面所有模式都没匹配到的内容，作用类似 `if` 里的 `else`。
- **`esac`**：语句结束关键字（就是 `case` 反着拼）。

#### shell 函数开发

函数的特点和 `alias` 类似：可以简化 Linux 命令操作，让整条命令更可读、更好用。

- 函数就是把要执行的 shell 命令组合成一个**函数体**。
- 给函数体起个名字，就是**函数名**。
- 函数名 + 函数体
- 以后想执行这段逻辑，直接敲函数名就行。

##### 用函数有什么好处？

使用方式

> 1. 先定义函数
> 2. 再使用函数

- 把相同的程序和定义封装成函数，可以减少代码量、提高开发效率。
- 用函数可以少写代码 —— 早写完早下班，美滋滋。
- 函数能提升程序的可读性和可维护性。

###### shell 函数定义语法

```
# 标准写法

function function_name() {
    函数体
    想执行的 Linux 命令...
    return return_value
}

# 懒人写法
# 用 function 关键字时，圆括号可以省略

function function_name {
    函数体
    想执行的命令...
    return return_value
}

# 超懒写法（shell 老手的自觉）
# 这种写法圆括号必须有

function_name() {

}
```

#### 函数执行的基本概念

- 执行 shell 函数直接写函数名即可，不需要加别的任何内容。
- 函数必须**先定义后执行**，因为 shell 脚本是自上而下加载的。
- 函数体内定义的变量叫局部变量。
- 函数体内要加 `return` 语句：作用是退出函数并给调用方（shell 脚本）一个返回值 —— 脚本里定义并调用函数后，执行结束就能通过 `$?` 拿到返回值。
- `return` 和 `exit` 的区别：
  - `return`：结束**函数**的执行并返回一个值（退出值 / 返回值）。
  - `exit`：结束**当前 shell 环境**并向 shell 返回一个值。
- 函数写在一个单独文件里时，需要用 `source` 加载。
- 函数内用 `local` 关键字定义局部变量。
